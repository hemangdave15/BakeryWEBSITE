1-> Here we are using Vite.js, to create our project as it is a fast service, and it also makes it easier to host

2-> Here we are using the concept of BEM (Block, Element, Modifier), which is a naming method in CSS to create a maintainable codebase.

3-> Here we are creating different CSS files for each section, and importing them directly into the style.css

4-> We are using AOS(Animate on Scroll) for the animation and transitions in this website

5-> We are using @media screen and (max-width: ) to make it responsive for different websites, in style.css